{
  "name": "shisuiji-app",
  "version": "1.0.0",
  "private": true,
  "description": "拾穗集 Android 混合壳（Capacitor）",
  "scripts": {
    "sync": "cap sync android",
    "copy": "cap copy android",
    "open": "cap open android",
    "build:debug": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
